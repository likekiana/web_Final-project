# 项目启动指南

## 1. 数据库初始化

### 1.1 手动执行数据库脚本

1. **打开命令提示符**（Win + R → 输入 `cmd` → 回车）

2. **切换到项目目录**：
   ```cmd
   cd F:\code\web_Final-project
   ```

3. **执行初始化脚本**：
   ```cmd
   mysql -u root -p3147 < 数据库初始化脚本.sql
   ```
   （注意：密码直接跟在 `-p` 后面，不要有空格）

4. **验证初始化结果**：
   ```cmd
   mysql -u root -p3147 -e "USE campus_forum; SHOW TABLES; SELECT COUNT(*) FROM categories; SELECT COUNT(*) FROM users WHERE role = 'superAdmin';"
   ```
   - 预期结果：显示 6 个表，categories 数量为 7，superAdmin 数量为 1

## 2. 启动后端服务

### 2.1 安装依赖

1. **切换到Python后端目录**：
   ```cmd
   cd backend
   ```

2. **安装Python依赖**：
   ```cmd
   pip install -r requirements.txt
   ```
   （这可能需要几分钟时间，取决于网络速度）

### 2.2 启动Django后端服务

```cmd
python manage.py runserver
```

- 预期输出：
  ```
  Watching for file changes with StatReloader
  Performing system checks...
  System check identified no issues (0 silenced).
  
  You have 18 unapplied migration(s). Your project may not work properly until you apply the migrations for app(s): accounts, admin, auth, contenttypes, sessions.
  Run 'python manage.py migrate' to apply them.
  
  December 12, 2025 - 10:00:00
  Django version 5.0, using settings 'campus_forum.settings'
  Starting development server at http://127.0.0.1:8000/
  Quit the server with CTRL-BREAK.
  ```

- 后端服务将在 `http://localhost:8000` 运行

### 2.3 数据库迁移（首次启动必需）

在启动后端服务之前，需要执行数据库迁移：

```cmd
python manage.py migrate
```

## 3. 启动前端服务

### 3.1 安装依赖

1. **打开新的命令提示符窗口**（保持后端服务运行）

2. **切换到前端目录**：
   ```cmd
   cd F:\code\web_Final-project\src\frontend
   ```

3. **安装依赖**：
   ```cmd
   npm install
   ```
   （这可能需要几分钟时间）

### 3.2 启动前端服务

```cmd
npm run dev
```

- 预期输出：
  ```
  VITE v5.0.0  ready in 1000 ms
  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
  ```

- 前端服务将在 `http://localhost:5173` 运行

## 4. 访问应用

1. **打开浏览器**

2. **访问前端应用**：
   ```
   http://localhost:5173
   ```

3. **使用初始账号登录**：
   - 用户名：`admin`
   - 密码：`admin123`
   - 建议登录后立即修改密码

## 5. 项目结构说明

### 5.1 Python后端结构

```
backend/
├── accounts/          # 用户账号应用
│   ├── models.py      # 数据模型
│   ├── views.py       # 视图（业务逻辑）
│   ├── urls.py        # 路由配置
│   └── serializers.py # 序列化器
├── ads/              # 广告应用
├── campus_forum/      # 项目配置
│   ├── settings.py    # 主配置文件
│   ├── urls.py        # 根路由配置
│   └── wsgi.py        # WSGI入口
├── content/           # 内容管理应用
├── likes/             # 点赞应用
├── reports/           # 举报应用
├── manage.py          # Django管理命令入口
└── requirements.txt   # Python依赖配置
```

### 5.2 前端结构

```
src/frontend/
├── src/
│   ├── App.jsx       # 主组件
│   └── main.jsx      # 入口文件
├── package.json      # 依赖配置
└── vite.config.js    # Vite配置
```

## 6. 常见问题排查

### 6.1 后端启动失败

- **端口被占用**：使用 `python manage.py runserver 8001` 尝试其他端口
- **数据库连接失败**：检查 `backend/campus_forum/settings.py` 中的数据库配置，确保密码正确
- **迁移失败**：确保数据库已初始化，尝试删除 `migrations` 文件夹后重新执行 `python manage.py makemigrations` 和 `python manage.py migrate`
- **依赖安装失败**：尝试使用 `pip install --upgrade pip` 更新pip，或使用虚拟环境

### 6.2 前端启动失败

- **依赖安装失败**：尝试删除 `node_modules` 和 `package-lock.json`，重新执行 `npm install`
- **端口被占用**：修改 `src/frontend/vite.config.js` 中的端口配置

### 6.3 无法访问应用

- 检查后端和前端服务是否都在运行
- 检查浏览器地址是否正确
- 检查防火墙是否阻止了端口访问

## 7. 开发说明

- **后端 API 地址**：`http://localhost:8000/api`
- **前端开发地址**：`http://localhost:5173`
- **Django 管理后台**：`http://localhost:8000/admin`（使用超级管理员账号登录）
- **数据库管理**：可以使用 MySQL Workbench 或 Navicat 等工具管理数据库

## 8. 后续开发建议

1. **完善用户认证**：添加邮箱验证、密码重置等功能
2. **增强帖子功能**：添加富文本编辑器、图片上传等功能
3. **优化前端界面**：完善各页面布局和交互效果
4. **添加测试**：编写单元测试和集成测试
5. **部署上线**：配置生产环境，部署到服务器

---

**祝你开发顺利！