# 数据库创建与初始化指南

## 1. 前提条件

- **已安装MySQL**：确保MySQL数据库已安装在您的计算机上
- **MySQL服务已启动**：确保MySQL服务处于运行状态
- **有管理员权限**：需要使用具有创建数据库权限的用户（如root）

## 2. 检查MySQL服务状态

### Windows系统

1. 按下 `Win + R` 打开运行窗口
2. 输入 `services.msc` 并按回车
3. 在服务列表中找到 `MySQL` 或 `MySQL80`（取决于版本）
4. 检查状态是否为「正在运行」，如果不是，右键点击并选择「启动」

### 其他系统（Linux/macOS）

```bash
# Linux
sudo systemctl status mysql

# macOS（使用Homebrew安装的MySQL）
brew services list | grep mysql
```

## 3. 连接到MySQL服务器

### 使用MySQL命令行客户端

1. 打开命令提示符（Windows）或终端（Linux/macOS）
2. 输入以下命令连接到MySQL服务器：

```bash
# 使用root用户连接
mysql -u root -p
```

3. 按回车后，系统会提示输入密码
4. 输入MySQL root用户的密码，按回车

### 使用MySQL Workbench（图形界面）

1. 打开MySQL Workbench
2. 点击「+」号创建新连接
3. 填写连接信息：
   - 连接名称：自定义（如「校园论坛」）
   - 主机名：localhost
   - 端口：3306（默认）
   - 用户名：root
   - 密码：点击「Store in Vault...」并输入密码
4. 点击「Test Connection」测试连接，成功后点击「OK」
5. 双击新创建的连接，进入MySQL Workbench主界面

## 4. 执行数据库初始化脚本

### 方法1：命令行执行

1. 在命令提示符或终端中，确保已连接到MySQL服务器
2. 输入以下命令执行脚本：

```bash
# 切换到脚本所在目录
cd F:\code\web_Final-project

# 执行脚本
mysql -u root -p campus_forum < 数据库初始化脚本.sql
```

3. 按回车后，系统会提示输入密码
4. 输入MySQL root用户的密码，按回车
5. 等待脚本执行完成，无错误信息则表示成功

### 方法2：在MySQL命令行客户端中执行

1. 连接到MySQL服务器后，输入以下命令：

```sql
-- 执行脚本文件
SOURCE F:/code/web_Final-project/数据库初始化脚本.sql;
```

2. 按回车执行，等待脚本执行完成
3. 出现「数据库初始化完成！」消息则表示成功

### 方法3：在MySQL Workbench中执行

1. 连接到MySQL服务器后，点击顶部菜单的「File」→「Open SQL Script」
2. 浏览到 `F:\code\web_Final-project\数据库初始化脚本.sql` 文件，点击「Open」
3. 点击工具栏中的「闪电」图标（Execute SQL Script）
4. 等待脚本执行完成，查看「Output」面板中的结果
5. 出现「1 row(s) returned」且无错误信息则表示成功

## 5. 验证初始化结果

### 命令行验证

连接到MySQL服务器后，输入以下命令验证：

```sql
-- 切换到campus_forum数据库
USE campus_forum;

-- 查看所有表
SHOW TABLES;

-- 验证初始板块数据
SELECT COUNT(*) FROM categories;
-- 预期结果：7

-- 验证初始管理员数据
SELECT COUNT(*) FROM users WHERE role = 'superAdmin';
-- 预期结果：1
```

### MySQL Workbench验证

1. 连接到MySQL服务器后，在「Navigator」面板中点击「Schemas」
2. 展开 `campus_forum` 数据库，查看所有表是否存在
3. 右键点击 `categories` 表，选择「Select Rows - Limit 1000」
4. 确认显示7条板块数据
5. 同样验证 `users` 表中是否存在管理员用户

## 6. 常见问题与解决方案

### 问题1：MySQL服务无法启动

**解决方案**：
- 检查MySQL配置文件是否正确
- 检查端口3306是否被占用
- 查看MySQL错误日志（通常位于MySQL安装目录的 `data` 文件夹中）

### 问题2：连接MySQL时提示「Access denied for user 'root'@'localhost'」

**解决方案**：
- 确认密码输入正确
- 如果忘记密码，需要重置MySQL root密码
- 确保root用户具有本地连接权限

### 问题3：执行脚本时提示「Can't connect to MySQL server on 'localhost' (10061)」

**解决方案**：
- 确认MySQL服务已启动
- 确认MySQL监听的是localhost（127.0.0.1）
- 检查防火墙是否阻止了MySQL连接

### 问题4：执行脚本时提示「Database 'campus_forum' doesn't exist」

**解决方案**：
- 检查脚本中是否包含 `CREATE DATABASE IF NOT EXISTS campus_forum` 语句
- 如果没有，可以手动创建数据库：
  ```sql
  CREATE DATABASE IF NOT EXISTS campus_forum
  DEFAULT CHARACTER SET utf8mb4
  DEFAULT COLLATE utf8mb4_unicode_ci;
  ```

## 7. 后续步骤

数据库初始化完成后，您可以：

1. 启动后端服务：
   ```bash
   cd src/backend
   npm install
   npm start
   ```

2. 启动前端服务：
   ```bash
   cd src/frontend
   npm install
   npm run dev
   ```

3. 访问前端应用：打开浏览器访问 `http://localhost:5173`

4. 使用初始管理员账号登录：
   - 用户名：admin
   - 密码：admin123
   - 建议登录后立即修改密码

## 8. 注意事项

- **密码安全**：初始密码 `admin123` 仅用于测试，生产环境中必须修改为强密码
- **备份数据**：定期备份数据库，防止数据丢失
- **权限管理**：生产环境中，不要使用root用户连接数据库，创建专门的应用用户
- **更新脚本**：如果数据库设计有变更，需要更新初始化脚本

---

**完成**：数据库创建与初始化已完成！

如果您在操作过程中遇到任何问题，请查阅MySQL官方文档或联系开发团队。